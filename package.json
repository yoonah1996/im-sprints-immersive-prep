{
  "name": "immersive-prep",
  "version": "1.0.0",
  "description": "Repository for preperation immersive course.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:example": "jest test-example/",
    "test:passme": "jest pass-me/",
    "test:watch": "jest --watch",
    "lint": "eslint **/*.js --quiet",
    "submit-test": "jest --json --outputFile=result.json"
  },
  "keywords": [
    "linter",
    "testing",
    "recursion",
    "underbar"
  ],
  "author": "codesatates",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.8.0",
    "jest": "^25.1.0"
  }
}
